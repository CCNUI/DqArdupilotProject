# Answer from claude-opus-4-1-20250805-thinking-16k
我来帮您详细配置6轴非对称倾转旋翼飞机。这是一个复杂的配置，我会尽量利用Ardupilot现有的VTOL功能。

## 第一部分：基础配置和参数设置

### 1. Mission Planner基础配置

首先在Mission Planner中进行以下设置：

```
# 基础参数设置
Q_ENABLE = 1                # 启用QuadPlane模式
Q_FRAME_CLASS = 1          # 设为自定义框架
Q_FRAME_TYPE = 1           # 自定义类型
Q_TILT_ENABLE = 1          # 启用倾转功能
Q_TILT_TYPE = 2            # 连续倾转类型
Q_TILT_MAX = 90            # 最大倾转角度（稍后会个别调整）
Q_TILT_RATE_DN = 35        # 向下倾转速度
Q_TILT_RATE_UP = 40        # 向上倾转速度
Q_TRANSITION_MS = 5000     # 过渡时间（毫秒）

# 6个电机输出配置
SERVO1_FUNCTION = 33       # Motor1
SERVO2_FUNCTION = 34       # Motor2  
SERVO3_FUNCTION = 35       # Motor3
SERVO4_FUNCTION = 36       # Motor4
SERVO5_FUNCTION = 37       # Motor5
SERVO6_FUNCTION = 38       # Motor6

# 6个倾转舵机配置
SERVO7_FUNCTION = 75       # TiltMotorFrontLeft (电机1倾转)
SERVO8_FUNCTION = 76       # TiltMotorFrontRight (电机2倾转)
SERVO9_FUNCTION = 41       # TiltMotorsFront (电机3倾转)
SERVO10_FUNCTION = 45      # TiltMotorsRear (电机4倾转)
SERVO11_FUNCTION = 46      # TiltMotorRearLeft (电机5倾转)
SERVO12_FUNCTION = 47      # TiltMotorRearRight (电机6倾转)

# 控制舵面配置
SERVO13_FUNCTION = 4       # Aileron
SERVO14_FUNCTION = 19      # Elevator
SERVO15_FUNCTION = 21      # Rudder
```

### 2. 自定义电机混控矩阵代码

创建一个新文件 `custom_tiltrotor_6axis.cpp`，放在 `ArduPlane` 目录下：

```cpp
#include "Plane.h"
#include <AP_Motors/AP_MotorsMatrix.h>

// 6轴非对称倾转旋翼配置
class Custom6AxisTiltrotor {
public:
    // 电机位置定义（相对于重心）
    struct MotorPosition {
        float x;  // 前后位置 (米)
        float y;  // 左右位置 (米)  
        float z;  // 上下位置 (米)
        float tilt_angle_max;  // 最大倾转角度
        float tilt_angle_min;  // 最小倾转角度（平飞）
    };

    // 6个电机的位置和倾转参数
    static const MotorPosition motor_positions[6];
    
    // 计算混控矩阵
    static void calculate_motor_matrix(float roll_in, float pitch_in, 
                                      float yaw_in, float throttle_in,
                                      float tilt_angle[6], float motor_out[6]);
    
    // 倾转角度补偿
    static void tilt_compensation(float target_tilt[6], float current_tilt[6], 
                                 float dt);
};

// 电机位置定义（根据您的实际布局修改）
const Custom6AxisTiltrotor::MotorPosition Custom6AxisTiltrotor::motor_positions[6] = {
    // {x, y, z, tilt_max, tilt_min}
    {0.5f,  -0.4f,  0.1f,  90.0f, 0.0f},   // 电机1：前左，稍高
    {0.5f,   0.4f,  0.1f,  90.0f, 0.0f},   // 电机2：前右，稍高
    {0.0f,  -0.6f, -0.05f, 75.0f, 0.0f},   // 电机3：中左，稍低（倾转受限）
    {0.0f,   0.6f, -0.05f, 75.0f, 0.0f},   // 电机4：中右，稍低（倾转受限）
    {-0.5f, -0.3f,  0.0f,  90.0f, 0.0f},   // 电机5：后左，中等高度
    {-0.5f,  0.3f,  0.0f,  80.0f, 0.0f},   // 电机6：后右，中等高度（倾转受限）
};

// 非对称混控矩阵计算
void Custom6AxisTiltrotor::calculate_motor_matrix(float roll_in, float pitch_in,
                                                  float yaw_in, float throttle_in,
                                                  float tilt_angle[6], float motor_out[6]) {
    // 计算每个电机的力矩贡献
    for (int i = 0; i < 6; i++) {
        const MotorPosition &pos = motor_positions[i];
        
        // 根据倾转角度计算推力方向
        float tilt_rad = radians(tilt_angle[i]);
        float thrust_vertical = cosf(tilt_rad);
        float thrust_forward = sinf(tilt_rad);
        
        // 计算力矩臂
        float roll_arm = pos.y * thrust_vertical;
        float pitch_arm = pos.x * thrust_vertical + pos.z * thrust_forward;
        float yaw_arm = pos.x * thrust_forward * 0.3f; // 倾转时的偏航贡献
        
        // 根据电机位置不对称性调整混控权重
        float position_factor = 1.0f + fabsf(pos.z) * 0.2f; // 高度差补偿
        
        // 混控输出
        motor_out[i] = throttle_in * thrust_vertical * position_factor
                     + roll_in * roll_arm * 0.5f
                     + pitch_in * pitch_arm * 0.5f
                     + yaw_in * yaw_arm * 0.3f;
        
        // 限幅
        motor_out[i] = constrain_float(motor_out[i], 0.0f, 1.0f);
    }
}

// 倾转补偿函数
void Custom6AxisTiltrotor::tilt_compensation(float target_tilt[6], 
                                             float current_tilt[6], float dt) {
    const float TILT_RATE_LIMIT = 60.0f; // 度/秒
    
    for (int i = 0; i < 6; i++) {
        // 应用倾转角度限制
        target_tilt[i] = constrain_float(target_tilt[i], 
                                        motor_positions[i].tilt_angle_min,
                                        motor_positions[i].tilt_angle_max);
        
        // 速率限制
        float tilt_error = target_tilt[i] - current_tilt[i];
        float max_change = TILT_RATE_LIMIT * dt;
        
        if (fabsf(tilt_error) > max_change) {
            current_tilt[i] += (tilt_error > 0) ? max_change : -max_change;
        } else {
            current_tilt[i] = target_tilt[i];
        }
    }
}
```

### 3. 倾转控制逻辑增强

创建文件 `tiltrotor_control_6axis.cpp`：

```cpp
#include "AP_HAL/AP_HAL.h"
#include "Plane.h"

// 6轴倾转控制主函数
void Plane::update_6axis_tiltrotor() {
    // 当前飞行模式
    bool in_vtol_mode = quadplane.in_vtol_mode();
    bool in_transition = quadplane.in_transition();
    
    // 倾转角度数组
    static float current_tilt[6] = {0};
    float target_tilt[6] = {0};
    
    // 根据飞行模式设置目标倾转角度
    if (in_vtol_mode && !in_transition) {
        // 垂直起降模式 - 所有电机垂直
        for (int i = 0; i < 6; i++) {
            target_tilt[i] = Custom6AxisTiltrotor::motor_positions[i].tilt_angle_max;
        }
        
        // 上升时舵面向下以减少阻力
        if (ahrs.get_accel_ef().z > 0.5f) {  // 正在上升
            SRV_Channels::set_output_scaled(SRV_Channel::k_elevator, -500);
            SRV_Channels::set_output_scaled(SRV_Channel::k_aileron, 0);
            SRV_Channels::set_output_scaled(SRV_Channel::k_rudder, 0);
        }
    } 
    else if (!in_vtol_mode && !in_transition) {
        // 固定翼模式 - 所有电机水平
        for (int i = 0; i < 6; i++) {
            target_tilt[i] = Custom6AxisTiltrotor::motor_positions[i].tilt_angle_min;
        }
        
        // 正常舵面控制
        // 舵面控制由标准固定翼控制处理
    }
    else if (in_transition) {
        // 过渡模式 - 渐进倾转
        float transition_progress = quadplane.get_transition_progress();
        
        for (int i = 0; i < 6; i++) {
            const auto &pos = Custom6AxisTiltrotor::motor_positions[i];
            float angle_range = pos.tilt_angle_max - pos.tilt_angle_min;
            
            // 不同电机不同的过渡曲线
            float motor_transition = transition_progress;
            
            // 前部电机先倾转
            if (pos.x > 0) {
                motor_transition = MIN(transition_progress * 1.2f, 1.0f);
            }
            // 后部电机延迟倾转
            else if (pos.x < 0) {
                motor_transition = MAX(0, (transition_progress - 0.2f) * 1.25f);
            }
            
            target_tilt[i] = pos.tilt_angle_max - (angle_range * motor_transition);
        }
    }
    
    // 应用倾转补偿和速率限制
    float dt = AP::scheduler().get_loop_period_s();
    Custom6AxisTiltrotor::tilt_compensation(target_tilt, current_tilt, dt);
    
    // 输出到倾转舵机
    output_tilt_servos(current_tilt);
    
    // 计算并输出电机推力
    calculate_and_output_motors(current_tilt);
}

// 输出倾转舵机PWM
void Plane::output_tilt_servos(float tilt_angles[6]) {
    // 将角度转换为PWM并输出
    static const struct {
        SRV_Channel::Aux_servo_function_t function;
        float angle_at_min_pwm;
        float angle_at_max_pwm;
    } servo_map[6] = {
        {SRV_Channel::k_tiltMotorLeft,      0, 90},   // SERVO7
        {SRV_Channel::k_tiltMotorRight,     0, 90},   // SERVO8
        {SRV_Channel::k_tiltMotorsFront,    0, 75},   // SERVO9
        {SRV_Channel::k_tiltMotorsRear,     0, 75},   // SERVO10
        {SRV_Channel::k_tiltMotorRearLeft,  0, 90},   // SERVO11
        {SRV_Channel::k_tiltMotorRearRight, 0, 80},   // SERVO12
    };
    
    for (int i = 0; i < 6; i++) {
        // 角度映射到-1到1的范围
        float angle_range = servo_map[i].angle_at_max_pwm - servo_map[i].angle_at_min_pwm;
        float normalized = (tilt_angles[i] - servo_map[i].angle_at_min_pwm) / angle_range;
        normalized = constrain_float(normalized, 0.0f, 1.0f);
        
        // 转换为-4500到4500的输出范围
        int16_t output = (normalized - 0.5f) * 9000;
        SRV_Channels::set_output_scaled(servo_map[i].function, output);
    }
}

// 计算并输出电机推力
void Plane::calculate_and_output_motors(float tilt_angles[6]) {
    // 获取控制输入
    float roll_in = rollController.get_servo_out() / 4500.0f;
    float pitch_in = pitchController.get_servo_out() / 4500.0f;
    float yaw_in = yawController.get_servo_out() / 4500.0f;
    float throttle_in = throttle_percentage() / 100.0f;
    
    // 计算电机输出
    float motor_out[6];
    Custom6AxisTiltrotor::calculate_motor_matrix(roll_in, pitch_in, yaw_in, 
                                                 throttle_in, tilt_angles, motor_out);
    
    // 输出到电机
    for (int i = 0; i < 6; i++) {
        // 转换为PWM (1000-2000)
        uint16_t pwm = 1000 + motor_out[i] * 1000;
        
        // 使用对应的电机通道输出
        SRV_Channel::Aux_servo_function_t motor_function = 
            (SRV_Channel::Aux_servo_function_t)(SRV_Channel::k_motor1 + i);
        SRV_Channels::set_output_pwm(motor_function, pwm);
    }
}
```

## 第二部分：处理非对称和不同倾转限制

### 4. 参数微调配置

在Mission Planner中添加以下参数来处理每个电机的特殊性：

```
# 电机1-6的倾转限制（通过舵机行程设置）
SERVO7_MIN = 1100    # 电机1倾转最小值（平飞）
SERVO7_MAX = 1900    # 电机1倾转最大值（垂起）
SERVO8_MIN = 1100    
SERVO8_MAX = 1900
SERVO9_MIN = 1100    
SERVO9_MAX = 1750    # 电机3只能倾转75度
SERVO10_MIN = 1100   
SERVO10_MAX = 1750   # 电机4只能倾转75度
SERVO11_MIN = 1100
SERVO11_MAX = 1900
SERVO12_MIN = 1100
SERVO12_MAX = 1800   # 电机6只能倾转80度

# 过渡期间的补偿参数
Q_TILT_YAW_ANGLE = 10   # 倾转时的偏航补偿角度
Q_TAILSIT_THSCMX = 5    # 最大推力缩放
Q_ASSIST_SPEED = 15     # 辅助速度阈值
```

### 5. 编译和安装步骤

1. **修改wscript文件**：
   在`ArduPlane/wscript`中添加新文件：
   ```python
   'custom_tiltrotor_6axis.cpp',
   'tiltrotor_control_6axis.cpp',
   ```

2. **修改Plane.h**：
   添加函数声明：
   ```cpp
   void update_6axis_tiltrotor();
   void output_tilt_servos(float tilt_angles[6]);
   void calculate_and_output_motors(float tilt_angles[6]);
   ```

3. **修改ArduPlane.cpp主循环**：
   在`fast_loop()`函数中添加：
   ```cpp
   if (quadplane.enabled() && quadplane.frame_class == AP_Motors::MOTOR_FRAME_CUSTOM) {
       update_6axis_tiltrotor();
   }
   ```

4. **编译命令**：
   ```bash
   ./waf configure --board=CubeOrange
   ./waf plane
   ```

## 第三部分：测试和校准流程

### 6. 地面测试步骤

1. **电机顺序测试**：
   ```
   在Mission Planner中：
   - 进入Motor Test
   - 依次测试Motor A-F
   - 确认对应关系正确
   ```

2. **倾转测试**：
   ```python
   # MAVProxy脚本测试倾转
   servo set 7 1500  # 测试电机1倾转到中位
   servo set 8 1500  # 测试电机2倾转到中位
   # ... 依次测试所有倾转舵机
   ```

3. **模式切换测试**：
   - QSTABILIZE模式：检查所有电机是否垂直
   - FBWA模式：检查所有电机是否水平
   - 检查过渡过程是否平滑

### 关键安全参数

```
# 安全相关参数
Q_M_HOVER_LEARN = 2     # 悬停学习
Q_ANGLE_MAX = 3000      # 最大倾斜角度
Q_A_RAT_RLL_P = 0.15    # Roll率控制P值
Q_A_RAT_PIT_P = 0.15    # Pitch率控制P值
Q_A_RAT_YAW_P = 0.18    # Yaw率控制P值

# 失效保护
FS_QBT_ENABLE = 1       # 启用QuadPlane失效保护
Q_LAND_SPEED = 50       # 紧急降落速度
```

这个配置充分利用了Ardupilot现有的倾转旋翼功能，通过自定义混控矩阵处理非对称布局，并通过舵机行程限制处理不同的倾转角度。建议先在仿真环境(SITL)中测试，确认逻辑正确后再上实机。
